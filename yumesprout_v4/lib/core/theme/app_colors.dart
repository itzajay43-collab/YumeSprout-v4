import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // Brand Colors
  static const Color primary = Color(0xFF6366F1);
  static const Color secondary = Color(0xFF8B5CF6);
  static const Color accent = Color(0xFFF472B6);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Text
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);

  // Borders
  static const Color border = Color(0xFFE5E7EB);

  // Progress
  static const Color progressBackground = Color(0xFFEEF2FF);

  // Cards
  static const Color card = Colors.white;

  // Character Background
  static const Color characterBackground = Color(0xFFEEF2FF);

  // Chips
  static const Color streakChip = Color(0xFFFFF4E5);
  static const Color xpChip = Color(0xFFEEF2FF);
}